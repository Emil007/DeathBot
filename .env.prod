# --- DISCORD EINSTELLUNGEN ---
# Das Zeichen, auf das der Bot reagiert (z.B. !start, !pick)
PREFIX=!

# Dein Bot Token (vom Developer Portal)
TOKEN=DEIN_DISCORD_BOT_TOKEN_HIER

# ID deines Servers (Rechtsklick auf Server -> ID kopieren)
SERVER=DEINE_SERVER_ID_HIER

# ID des Kanals, in den Berichte & Todesmeldungen gepostet werden
CHANNEL=DEINE_KANAL_ID_HIER

# Deine eigene User-ID (für !admincheck und manuelle !pick @User Befehle)
ADMIN_ID=DEINE_USER_ID_HIER


# --- SPIELREGELN ---
# Wie viele Promis darf jeder Spieler wählen?
PICK_LIMIT=25

# Dürfen mehrere Spieler denselben Promi haben? (1 = Ja, 0 = Nein)
ALLOW_SAME_PICK=1


# --- KÜNSTLICHE INTELLIGENZ (xAI / Grok) ---
# 1 = Eingeschaltet, 0 = Ausgeschaltet
OPENAI_ENABLED=1

# Dein API Key von console.x.ai
OPENAI_PAINT_API_KEY=xai-DEIN_API_KEY_HIER

# Die URL für xAI (WICHTIG: Nicht ändern für Grok)
OPENAI_PAINT_URL=https://api.x.ai/v1

# Das Modell, das genutzt werden soll (z.B. grok-2-vision-1212 oder grok-beta)
OPENAI_PAINT_MODEL=grok-2-vision-1212


# --- OPTIK / KOSMETIK ---
# Welches Emoji soll bei Todes-Alarm verwendet werden?
ALERT_EMOJI=🚨

# Wie oft soll das Emoji wiederholt werden?
ALERT_EMOJI_REPEAT=3